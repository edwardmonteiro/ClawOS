# ClawOS Message Bus Configuration
# /etc/claw/bus.conf

# Socket path
socket = /run/claw/bus.sock

# Maximum subscriptions
max_subscriptions = 1024

# Maximum message size (bytes)
max_message_size = 65536

# Log level
log_level = info
